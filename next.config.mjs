/** @type {import('next').NextConfig} */
const nextConfig = {
    output: "export",
    basePath: "/my-game", // Match your GitHub repository name
    assetPrefix: "/my-game/",
  };
  
  export default nextConfig;
  